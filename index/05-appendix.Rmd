`r if(knitr:::is_latex_output()) '\\appendix'`

`r if(!knitr:::is_latex_output()) '# (APPENDIX) Appendix {-}'` 

# Tables des analyses d'exomes {#sssdd}

\blandscape


```{r annexetabafterdnah1}

# kable(tab_modelow(df_bigmmaf_filter2) %>% arrange(Patient, Gene),
#       caption = "Liste des variants restant après avoir retirée ceux impactant spécifiquement le patient ...",
#       longtable = TRUE,
#       booktabs = TRUE)

```

\elandscape
\newpage

```{r testdfdd}
# 
#   
# 
# kable(df_bigmmaf_filter5 %>% filter(IMPACT == "HIGH") %>%
#   mutate(Coordinates = paste0(CHR,":",POSITION,":",REF,"->",ALT)) %>%
#   distinct(RUN, Coordinates, SYMBOL, Consequence) %>%
#     select(Patient = RUN, Coordinates, Gene = SYMBOL, Consequence) %>% 
#     arrange(Gene),
#   booktabs = TRUE,
#   longtable = TRUE, 
#   caption = "aaaaaaaaaa")


```

# The Second Appendix, for Fun
